import { FC } from "react";
interface ISetupPageProps {};

const SetupPage: FC<ISetupPageProps> = async (props) => {
    
    
    
    return (
        <div>
            Create a Server
        </div>
    );
}

export default SetupPage;